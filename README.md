# FamiliarFaceProject

The project is a web app to find a "familiar face" from your friends list in a Facebook.

# Front-End
Using Android wrapper from the website. Also using Java to process the image from Phone Photo Library to information for the model or Python back-end.

# Back End
Using Python to find the user id in Facebook from the your loged in Facebook friends list. The output will be an dictionary or "arrayList". The key is the user name from the "recognized" photo and the value is the "certainty" this is the peron in the photo.

The data between passed between the Front-End and Back-End is sent via a file with the written information from Python to Java and visa versa
